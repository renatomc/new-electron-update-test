{
  "name": "example-medium",
  "version": "1.1.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win --publish never",
    "deploy": "electron-builder --win --publish always"
  },
  "devDependencies": {
    "electron": "^12.0.4",
    "electron-builder": "^22.10.5"
  },
  "dependencies": {
    "electron-updater": "^4.3.8"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/renatomc/new-electron-update-test.git"
  }
}
